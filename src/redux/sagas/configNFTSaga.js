import { put, takeLatest } from 'redux-saga/effects';
// import { getConfigNFTAPI } from '../../services/configNFTRequest';
// import { configNFTAction, configNFTtActionTypes } from '../actions/configNFTActionCreators';

// function* getConfigNFT() {
//   try {
//     const response = yield getConfigNFTAPI();
//     yield put(configNFTAction.getConfigNFTSuccess(response));
//   } catch (e) {
//     yield put(configNFTAction.getConfigNFTError(e));
//   }
// }

// export function* watchConfigNFT() {
//   yield takeLatest(configNFTtActionTypes.GET_CONFIG_NFT_REQUEST, getConfigNFT);
// }
