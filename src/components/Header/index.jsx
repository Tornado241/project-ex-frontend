import React, { useState } from 'react';
import { Badge, Drawer, Image, Layout, Popover, Typography } from 'antd';

const { Header } = Layout;

const HeaderCommon = ({ ...props }) => {
  return <Header className="header">123213</Header>;
};
export default HeaderCommon;
